
export interface SalesmanData {
    id: string
    name: string
    last_name: string
    email: string
    phone: string
    sex: string
};

export interface SalesmanId {
    id: string
};

export interface Salesman {
    name: string
    last_name: string
    email: string
    phone: string
    sex: string
};